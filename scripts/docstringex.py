def add_one(x):
    """Add one to an input variable and return the output.

    Args:
        x (_type_): the input either as a float or integer
        
    Returns: 
        int: the input with one added
        
        
    interesting: assert xisinstance(x, int)
    """
    return x + 1

